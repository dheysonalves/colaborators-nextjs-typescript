import styled from '@emotion/styled'

export const FooterContainer = styled.div`
	background-color: ${(props) => props.theme.light.neutral.background};
	display: flex;
	justify-content: flex-end;
	padding: 20px;
`
